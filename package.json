{
  "name": "elena-site",
  "private": true,
  "scripts": {
    "build": "node build.js && eleventy && node build.js post",
    "start": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6"
  }
}